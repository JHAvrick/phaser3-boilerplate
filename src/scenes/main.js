import Phaser from 'phaser';

class Main extends Phaser.Scene {
    constructor () {
        super({key: 'Main' });
    }

    init (){console.log("Main...")}

    create(){
        
    }

}

export default Main;